// function name0ffunction(parameter....) {
//     function code
// }

const mul = (num1, num2) =>  num1 * num2;

console.log('The result of mul is', mul(5, 3));

const createArray = (num1, num2) => [num1, num2];

console.log('The result of calling createArray is: ', createArray(4, 5));
