//This is a pseudo code explaining the task.
int main()
{
//validate number of arguments with argc, argv
if (no_of_args > 2)
	printf("error msg");
exit(EXIT_FAILURE);
//failure to open file for any reason
fopen("file_name", r);

stacks_s *head = NULL;

// reads line by line
line = getCurrentline()
	if line[0] == "push" and line[1]:
//cehcks if the next item on the line is an integer -> line[1]
//add the integer to the stack
//create a node -> malloc and check if malloc failed. if it failed exit(EXIT_FAILURE)
//add item to the doubly linked list.
}
